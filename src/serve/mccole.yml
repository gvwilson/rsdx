title: "Build a Web Service"
tag: "Build a small Flask web server to display plate data."
syllabus:
-   Routing HTTP requests to functions.
-   Separating routing from model.
-   Creating HTML pages from [Jinja][jinja] templates.
-   Serving static files (and what that means).
exclude:
-   static
-   templates
